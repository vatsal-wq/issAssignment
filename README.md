src/ contains Java Core files
sqlFiles contain .sql files with queries; use command `source <.sql file>` to run (ensure login password are setup appropriately)
HTML/ contains basic client side programming
